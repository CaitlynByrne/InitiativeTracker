# Development Dockerfile with hot reload for DM Console
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server with host exposed for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
